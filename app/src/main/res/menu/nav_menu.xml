<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Application Name (Display Only) -->
    <item
        android:id="@+id/nav_app_name"
        android:title="Schedule App"
        android:enabled="false"
        android:icon="@drawable/home"
    android:titleTextAppearance="?android:attr/textAppearanceMedium" />

    <!-- Divider -->
    <item
        android:id="@+id/divider1"
        android:title=""
        android:enabled="false"
        android:icon="@android:drawable/divider_horizontal_bright" />

    <!-- Calendar Item -->
    <item
        android:id="@+id/nav_calendar"
        android:title="Lịch"
        android:icon="@drawable/img" /> <!-- Add a calendar icon here -->

    <!-- Divider -->
    <item
        android:id="@+id/divider2"
        android:title=""
        android:enabled="false"
        android:icon="@android:drawable/divider_horizontal_bright" />

    <!-- Contacts Item -->
    <item
        android:id="@+id/nav_contacts"
        android:title="Liên hệ"
        android:icon="@drawable/img" /> <!-- Add a contacts icon here -->

    <!-- Divider -->
    <item
        android:id="@+id/divider3"
        android:title=""
        android:enabled="false"
        android:icon="@android:drawable/divider_horizontal_bright" />

    <!-- Exit Item with X Icon -->
    <item
        android:id="@+id/nav_exit"
        android:title="Thoát"
        android:icon="@android:drawable/ic_menu_close_clear_cancel" />
</menu>
